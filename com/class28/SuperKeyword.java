package com.class28;

public class SuperKeyword {
	/*
	 * super() --> Calling Immediate Parent class constructor
	 * ALWAYS MUST be the first line inside a constructor
	 * this() --> Calling CURRENT class constructor
	 * ALWAYS MUST be the first line inside a constructor
	 */
	
	/*
	 * Can we have this() and super() together in 1 constructor --> NO
	 * super. with variables and methods
	 * super.variableName --> refers to the parent class variable
	 * (if parent class and child class have same variable name and you want to differentiate/access)
	 */
	
	/*
	 * this.variables --> refers to the current class variable (to differentiate)
	 * this.methodName --> to call current class method
	 * super.methodName --> to call parent class method
	 */
	
	/*
	 * 
	 */
}
