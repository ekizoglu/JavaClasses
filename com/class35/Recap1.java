package com.class35;
import java.util.*; 

public class Recap1 {
	//Recap of Maps
	/*
	 * HashMap: Map in which an order is not maintained
	 * TreeMap: Map in which key objects will be stored in ascending order
	 * LinkedHashMap: Map in which order is preserved as entry objects entered into the map
	 * HashTable 
	 * 
	 * to store --> .put (K,V);
	 * to retrieve --> .get(K);
	 * 
	 * To Retrieve all keys
	 * .keySet() --> Returns a "Set" of all keys from the map (keys are always unique)
	 * 
	 * To Retrieve all values
	 * .values() --> Returns a "Collection" of values (values can be duplicates and they are not ordered) 
	 * 
	 */
	public static void main(String[] args) {
		
	}
}
