package com.class34;

public class ArrayListLinkedListNotes {

/*
 	ArrayList vs Linked List
 	1. ArrayList uses dynamic array while linked list uses doubly linked list
 	2. ArrayList is the best option for retrieving, while LinkedList is best for adding and removing
 	
 	ArrayList vs Array
 	1. Array is fixed in size --> static array, while ArrayList is a dynamic array. 
 	2. Array contains primitives and non-primitives, while ArrayList can contain only objects,
 	ArrayList can contain primitives only through corresponding wrapper classes (i.e. Integer, Character etc)
 	3. Finding the size of ArrayList --> ArrayList.size();
 	   Finding the size of Array --> array.length;
 	
 	LIST Interface -- is an Ordered collection that allows duplicates. 
 */
}
