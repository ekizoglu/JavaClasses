package com.class36;

import java.util.Map;

public class MapNotes {
	/*
	 * Map is a collection of entry objects which are key and value pair objects
	 * Map is not a part of collection framework
	 * 
	 * Every Key in the map holds unique objects
	 * 
	 * HashMap--> Order is not maintained
	 * TreeMap--> Key objects stored in ascending order
	 * LinkedHashMap --> Insertion order is preserved on key objects
	 * HashTable --> Similar to the HashMap (Legacy Class)
	 * 
	 * Map<K,V> variableName = new MapChildObject<>();
	 */
}
