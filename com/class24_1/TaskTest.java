package com.class24_1;

import com.class24.Task;

public class TaskTest {
	
	/*
	Task defaultTask = new Task(); //need access (default constructor in another package)
	Task privateTask = new Task(2); //need access (private constructor)
	Task protectedTask = new Task(4, "task4", 'D'); //need access (protected constructor in another package)
	*/
	Task publicTask = new Task(3,"task3");
}
